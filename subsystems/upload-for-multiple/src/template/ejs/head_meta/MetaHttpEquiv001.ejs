<!--
Project: upload-for-multiple
FileDirPath: src/template/ejs/head_meta/MetaHttpEquiv001.ejs
Author: 12278
Email: 1227839175@qq.com
IDE: WebStorm
CreateDate: 2024-1-1 00:00:00 星期一
-->
<!--以下是http-equiv Start-->
<!--http-equiv表示定义一个“pragma”指令，该属性被命名为http-equiv(alent)因为所有允许的值都是特定的HTTP标头的名称。-->
<!--
该meta项用途：
设定网站的到期时间，一旦网页过期，必须到服务器上重新传输，指定Expires值为一个早已过去的时间，那么访问此网时若重复在地址栏按回车，那么每次都会重复从服务器访问。

值说明：
0：表示立即过期，始终从服务器获取。
60：表示在60秒后过期。
-1：表示在任何时候都不能被Cache存储。

注意：
必须使用GMT的时间格式，如：Wed, 21 Oct 2015 07:28:00 GMT。

详细见：
https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Expires
-->
<% if(expires !== null && expires !== undefined){ %>
    <meta http-equiv = 'Expires'
          content = '<%= expires %>' />
<% } %>
<!--
该meta项用途：
cookie过期设定，如果网页过期，存盘的cookie将被删除。需要注意的也是必须使用GMT时间格式。

值如：cookievalue=xxx;expires=Fri, 12 Jan 2001 18:18:18 GMT;path=/

注意：
必须使用GMT的时间格式，如：Fri, 12 Jan 2001 18:18:18 GMT。

详细见：
https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie
-->
<% if(setCookieExpires !== null && setCookieExpires !== undefined){ %>
    <meta http-equiv = 'Set-Cookie'
          content = '<%= setCookieExpires %>' />
<% } %>
<!--
该meta项用途：
指定是否显示图片工具栏。

值说明：
true：代表显示。
false：代表不显示。
-->
<meta http-equiv = 'ImageToolBar'
      content = 'true' />
<!--
该meta项用途：
显示窗口的设定。

值说明：
_top：强制页面在当前窗口以独立页面显示，可以用来防止别人在框架里调用自己的页面。
_self：它使得目标文档载入并显示在相同的框架或者窗口中作为源文档。
_blank：浏览器总在一个新打开、未命名的窗口中载入目标文档。
_parent：这个目标使得文档载入父窗口或者包含来超链接引用的框架的框架集，如果这个引用是在窗口或者在顶级框架中，那么它与“_self”等效。
-->
<meta http-equiv = 'Window-Target'
      content = '_top' />
<!--
该meta项用途：
告诉浏览器页面的语言方式。

值说明：
zh-CN：简体中文。
-->
<meta http-equiv = 'Content-Language'
      content = 'zh-CN' />
<!--
该meta项用途：
是否启用DNS预解析。

值说明：
on：启用。
off：不启用。

注意：
虽然使用DNS Prefetch能够加快页面的解析速度，但是也不能滥用，因为有开发者指出禁用“DNS预读取”能节省每月100亿的DNS查询。
-->
<meta http-equiv = 'X-DNS-Prefetch-Control'
      content = 'on' />
<!--
该meta项用途：
表示允许跨域访问。

详细见：
https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Access-Control-Allow-Origin
-->
<meta http-equiv = 'Access-Control-Allow-Origin'
      content = '*' />
<!--
详细见：
https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Access-Control-Request-Headers
-->
<meta http-equiv = 'Access-Control-Request-Headers'
      content = 'deno-custom-file-sri, Authorization, Accept, Content-Type, Content-Language, Accept-Language, Cache-Control' />
<!--
详细见：
https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Access-Control-Request-Method
-->
<meta http-equiv = 'Access-Control-Request-Method'
      content = 'GET, HEAD, POST, PUT, DELETE, CONNECT, OPTIONS, TRACE, PATCH' />
<!--
该meta项用途：
W3C网页规范，指明页面中脚本的类型。
-->
<meta http-equiv = 'Content-Script-Type'
      content = 'text/javascript' />
<!--
该meta项用途：
声明文档的MIME类型和字符编码，如果指定，内容属性必须具有值“text/html;charset=utf-8”，这等效于指定了charset属性的meta元素，并且对文档中的位置具有相同的限制。

注意：
只能在以“text/html”提供的文档中使用，不能在以“XML”MIME类型提供的文档中使用。
-->
<meta http-equiv = 'Content-Type'
      content = 'text/html;charset=utf-8' />
<!--
该meta项用途：
Cache-Control指定请求和响应遵循的缓存机制，在请求消息或响应消息中设置Cache-Control并不会修改另一个消息处理过程中的缓存处理过程。
请求时的缓存指令包括：no-cache、no-store、max-age、max-stale、min-fresh、only-if-cached。
响应消息中的指令包括：public、private、no-cache、no-store、no-transform、must-revalidate、proxy-revalidate、max-age。

值说明：
public：指示响应可被任何缓存区缓存。
private：指示对于单个用户的整个或部分响应消息，不能被共享缓存处理，这允许服务器仅仅描述当用户的部分响应消息，此响应消息对于其他用户的请求无效。
no-cache：指示请求或响应消息不能缓存。
no-store：用于防止重要的信息被无意的发布，在请求消息中发送将使得请求和响应消息都不使用缓存。
max-age：指示客户机可以接收生存期不大于指定时间（以秒为单位）的响应。
min-fresh：指示客户机可以接收响应时间小于当前时间加上指定时间的响应。
max-stale：指示客户机可以接收超出超时期间的响应消息，如果指定max-stale消息的值，那么客户机可以接收超出超时期指定值之内的响应消息。
no-siteapp：禁止百度转码。

注意：
打开新窗口时，值为private、no-cache、must-revalidate，那么打开新窗口访问时都会重新访问服务器，而如果指定了max-age值，那么在此值内的时间里就不会重新访问服务器，例如：Cache-control: max-age=5（表示当访问此网页后的5秒内再次访问不会去服务器）。
在地址栏回车时，值为private、must-revalidate则只有第一次访问时会访问服务器，以后就不再访问，值为no-cache，那么每次都会访问，值为max-age，则在过期之前不会重复访问。
按后退按扭时，值为private、must-revalidate、max-age，则不会重访问，值为no-cache，则每次都重复访问。
按刷新按扭时，无论为何值，都会重复访问，Cache-control值为no-cache时，访问此页面不会在Internet临时文件夹留下页面备份。

详见：
https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meta#attributes
https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control
-->
<meta http-equiv = 'Cache-Control'
      content = 'no-cache' />
<!--
该标头的值的格式比较特别，必需是用“双引号”括起来，这时就会出现字符串嵌套字符串的情况。

详见：
https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Clear-Site-Data
-->
<!--
<meta http-equiv = 'Clear-Site-Data' content = '"cache", "cookies", "storage"' />
-->
<!--
该meta项用途：
是否开启cleartype显示效果，微软的一种让字体清晰的能力。
-->
<meta http-equiv = 'cleartype'
      content = 'on' />
<!--
设定进入页面时的特殊效果，这个功能即FrontPage中的“格式/网页过渡”。

值如：revealTrans(duration=5,transtion=8)
duration：持续时间，单位秒。
transtion：过渡方式，它的值为0到23，分别对应24种过渡方式。
如下表：
0：盒状收缩
1：盒状放射
2：圆形收缩
3：圆形放射
4：由下往上
5：由上往下
6：从左至右
7：从右至左
8：垂直百叶窗
9：水平百叶窗
10：水平格状百叶窗
11：垂直格状百叶窗
12：随意溶解
13：从左右两端向中间展开
14：从中间向左右两端展开
15：从上下两端向中间展开
16：从中间向上下两端展开
17：从右上角向左下角展开
18：从右下角向左上角展开
19：从左上角向右下角展开
20：从左下角向右上角展开
21：水平线状展开
22：垂直线状展开
23：随机产生一种过渡方式

注意：
所在页面不能够是一个frame、iframe页面。
-->
<% if(pageEnter !== null && pageEnter !== undefined){ %>
    <meta http-equiv = 'Page-Enter'
          content = '<%= pageEnter %>' />
<% } %>
<!--
设定离开页面时的特殊效果，这个功能即FrontPage中的“格式/网页过渡”。

值如：revealTrans(duration=5,transtion=8)
duration：持续时间，单位秒。
transtion：过渡方式，它的值为0到23，分别对应24种过渡方式。
如下表：
0：盒状收缩
1：盒状放射
2：圆形收缩
3：圆形放射
4：由下往上
5：由上往下
6：从左至右
7：从右至左
8：垂直百叶窗
9：水平百叶窗
10：水平格状百叶窗
11：垂直格状百叶窗
12：随意溶解
13：从左右两端向中间展开
14：从中间向左右两端展开
15：从上下两端向中间展开
16：从中间向上下两端展开
17：从右上角向左下角展开
18：从右下角向左上角展开
19：从左上角向右下角展开
20：从左下角向右上角展开
21：水平线状展开
22：垂直线状展开
23：随机产生一种过渡方式

注意：
所在页面不能够是一个frame、iframe页面。
-->
<% if(pageExit !== null && pageExit !== undefined){ %>
    <meta http-equiv = 'Page-Exit'
          content = '<%= pageExit %>' />
<% } %>
<!--
设定页面重定向、刷新之类的操作。
值，如：0;url=https://www.baidu.com
0：表示经过0秒后重定向到https://www.baidu.com。
也可以设置成自我刷新当前页面。
-->
<% if(refresh !== null && refresh !== undefined){ %>
    <meta http-equiv = 'refresh'
          content = '<%= refresh %>' />
<% } %>
<!--以上是http-equiv End-->
